// BackgroundLayer, blockLayer, spriteLayer, uiLayer

// middlewere i pasan argument:
  // prev => args, values
  // curr => args,
  // next => args, ret, values

// crawler ian da pode ferma al crawling...
// bubbleKey
// captureKey = onKey

// in casu par fa layer cun livei al po esa an Map e array

// da fa...
// capi cosa ca al fa update,
// agiüngia mappa
// capi useBuffer = true
// capi ben par i children

image(img, x, y)
image(img, x, y, w, h)
image(img, x, y, w, h, sx, sy)
image(img, x, y, w, h, sx, sy)

image(args + {trusted: true, drawHitbox: true})
